local original = game:GetService("Workspace").StartingZone.Buildings.Gunsmith.RevolverTable:FindFirstChild("Revolver")
if original then
    local clone = original:Clone()
    clone.Parent = original.Parent
    clone.Name = "Revolver_Copy"
end
