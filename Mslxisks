for _, v in pairs(game:GetService("ReplicatedStorage"):GetDescendants()) do
    if v:IsA("RemoteEvent") then
        print(v.Name .. " (" .. v:GetFullName() .. ")")
    end
end
print("Список готов, кидай мне!")
