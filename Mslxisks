local player = game.Players.LocalPlayer
local runService = game:GetService("RunService")
local logData = ""

local function addLog(text)
    logData = logData .. text .. "\n"
    print(text) -- Лог в консоль (можно скопировать)
end

local function trackHP()
    for _, npc in pairs(workspace:GetChildren()) do
        if npc:IsA("Model") and npc:FindFirstChild("Humanoid") then
            local humanoid = npc.Humanoid
            local hp = humanoid.Health
            local maxHp = humanoid.MaxHealth

            addLog("NPC: " .. npc.Name .. " | HP: " .. hp .. "/" .. maxHp)

            -- Проверяем, получает ли NPC урон
            humanoid.HealthChanged:Connect(function(newHp)
                addLog("NPC: " .. npc.Name .. " | Новый HP: " .. newHp .. "/" .. maxHp)
            end)
        end
    end
end

runService.Heartbeat:Connect(trackHP)
