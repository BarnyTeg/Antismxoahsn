local ScreenGui = game:GetObjects("rbxassetid://ВАШ_ЭКСПЛОИТ_ГУИ_ID")[1]
ScreenGui.Parent = game:GetService("CoreGui")

local function getObjectInfo(obj)
    return string.format("Class: %s | Name: %s | Path: %s\nMemory: %sMB", 
        obj.ClassName, 
        obj.Name, 
        obj:GetFullName(),
        math.round(game:GetService("Stats"):GetTotalMemoryUsageMb()/1000, 2)
    )
end

workspace.DescendantAdded:Connect(function(obj)
    local Button = TemplateButton:Clone()
    Button.Text = "🚩 "..obj.Name..(obj:IsA("BasePart") and " [PART]" or "")
    Button.MouseButton1Click:Connect(function()
        syn.write_clipboard(tostring(obj:GetDebugId())) -- Для эксплойтов
    end)
    Button.Parent = ScrollingFrame
end)
