for _, remote in ipairs(game:GetDescendants()) do
    if remote:IsA("RemoteFunction") then
        remote.OnClientInvoke = function(...)
            print("[REMOTE FUNCTION]", remote:GetFullName(), "| Аргументы:", ...)
        end
    end
end
