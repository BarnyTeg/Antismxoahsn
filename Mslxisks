for _, remote in ipairs(game:GetDescendants()) do
    if remote:IsA("RemoteEvent") then
        remote.OnClientEvent:Connect(function(...)
            print("[REMOTE]", remote:GetFullName(), "| Аргументы:", ...)
        end)
    end
end
